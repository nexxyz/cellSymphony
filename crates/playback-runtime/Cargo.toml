[package]
name = "playback-runtime"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
platform-core = { path = "../platform-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
