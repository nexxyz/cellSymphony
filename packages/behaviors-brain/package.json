{
  "name": "@cellsymphony/behaviors-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Brian's Brain cellular automaton — three-state (alive/dying/dead) with fire threshold control",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "node -e \"process.exit(0)\"",
    "format:check": "node -e \"process.exit(0)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "test:coverage": "c8 --reporter=text --reporter=lcov tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "tsx": "^4.22.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@cellsymphony/behavior-api": "workspace:*",
    "@cellsymphony/device-contracts": "workspace:*"
  }
}
