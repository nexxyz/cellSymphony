# Cell Symphony hardware configuration
# Enable I2C, SPI, and playback-only I2S for PCM5102 DAC
dtparam=i2c_arm=on
dtparam=spi=on
dtparam=audio=off
enable_uart=0
dtoverlay=i2s-dac-no20

# Increase GPU memory for display (if using OLED via SPI)
gpu_mem=32
