# Cell Symphony hardware configuration
# Enable I2C, SPI, and I2S for PCM5102 DAC
dtparam=i2c_arm=on
dtparam=spi=on
dtparam=i2s=on
dtoverlay=hifiberry-dac

# Increase GPU memory for display (if using OLED via SPI)
gpu_mem=32
